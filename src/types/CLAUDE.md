# Types

The types here are the compile-time half of the data layer — the synced
`payload-types.ts`, the endpoint contract files, and the zod schemas kept aligned
with them. The rules that govern them live in
[`src/config/api/CLAUDE.md`](../config/api/CLAUDE.md).
