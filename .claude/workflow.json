{
  "$comment": "This file holds only per-repo values, for the sydevs `workflow` plugin and its skills. The skills hold the rules. docs/ holds the reasoning.",
  "packageManager": "pnpm",
  "leanGate": {
    "command": ".claude/skills/pr-prep/check.sh",
    "full": ".claude/skills/pr-prep/check.sh --full",
    "contents": "lint + typecheck + test:run"
  },
  "contractStep": {
    "description": "Refresh the generated CMS and OpenAPI contracts from SahajCloud main.",
    "command": "pnpm types:cms && pnpm types:openapi"
  },
  "securityReview": {
    "triggerPattern": "src/config/api/|src/types/event|src/Widget\\.tsx|src/App\\.tsx|src/lib/shape/routing|src/lib/atlas-history|src/router\\.tsx|src/loader/|src/lib/shape/lexical|src/lib/shape/path|src/lib/shape/href|src/components/atoms/Link/|src/components/organisms/EventDetails/|src/styles/|src/lib/scope|scripts/[^/]*css|postcss\\.config\\.js|src/config/attributes|src/lib/report|package\\.json|pnpm-lock\\.yaml|patches/|(^|/)\\.env|vite\\.config\\.ts",
    "contentPattern": "dangerouslySetInnerHTML|dompurify|DOMPurify|\\.innerHTML",
    "contentPaths": [
      "src"
    ],
    "rationale": "docs/security-triggers.md"
  },
  "worktreeSetup": [
    "pnpm install"
  ],
  "worktreeNotes": "Copy .env.local from the main checkout when the branch needs it.",
  "deployTarget": "Cloudflare Pages (sahajatlas, sahajatlas-design)",
  "devServer": {
    "command": "pnpm dev --port {port}",
    "basePort": 5174,
    "healthPath": "/",
    "isolateDatabase": false
  },
  "generatedFiles": [
    {
      "pattern": "^dist/",
      "reason": "`dist/` is build output from `pnpm build`. Never edit it by hand \u2014 change the source under `src/` and rebuild."
    },
    {
      "pattern": "^src/types/payload/",
      "reason": "`src/types/payload/` is generated by `pnpm types:cms`, which curls the schema from SahajCloud's main branch. To change these types the upstream SahajCloud schema must change and be merged first \u2014 see /cross-repo-issue."
    },
    {
      "pattern": "^pnpm-lock\\.yaml$",
      "reason": "`pnpm-lock.yaml` is generated by pnpm. Change dependencies with `pnpm add` / `pnpm remove` / `pnpm update` and let pnpm rewrite the lockfile."
    }
  ],
  "prAllowlistGlobs": [
    "package.json",
    "pnpm-lock.yaml",
    "**/*.md",
    ".claude/**",
    "src/types/payload/**",
    "public/locales/**"
  ]
}
