# Locales

Translation bundles loaded over HTTP by `i18next-http-backend`. The conventions
that govern them — namespaces, key shape, the budget — live in
[`src/config/CLAUDE.md`](../../src/config/CLAUDE.md).
