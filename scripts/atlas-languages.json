{
  "note": [
    "The languages an operator has enabled on SahajCloud's Sahaj Atlas configuration global",
    "(sydevs/SahajCloud#645). Refreshed with `pnpm sync:atlas-languages --write`; read by",
    "src/config/i18n-options.test.ts, which fails if any of them has no bundle in",
    "public/locales/. That is the whole job: the CMS owns which languages the atlas is OFFERED",
    "in, this build owns which ones it can actually render, and a language enabled with no",
    "bundle renders English while SahajCloud's hreflang promises a page in it.",
    "",
    "NOTHING UNDER src/ MAY IMPORT THIS FILE. It is an observation, not configuration. The",
    "widget reads the live global at runtime and falls back to `shippedLanguages` when it can't",
    "— a stale third opinion about what an operator wanted would be worse than no opinion.",
    "",
    "`syncedAt` is the day somebody last checked this against production — null would mean",
    "never. It is stamped on every `--write`, including one that changed nothing, because a",
    "snapshot that has matched for a year and one nobody has ever verified are otherwise the",
    "same file.",
    "",
    "The set below is currently identical to `shippedLanguages`, and that is a real reading",
    "rather than a placeholder: SahajCloud's field defaults to the launch ten and no operator",
    "has narrowed it yet. So the superset assertion passes trivially TODAY and stops doing so",
    "the moment anyone enables an eleventh — which is exactly when it needs to fire."
  ],
  "syncedAt": "2026-08-25",
  "languages": ["cs", "de", "en", "es", "fr", "hu", "nl", "pt-BR", "ru", "uk"]
}
